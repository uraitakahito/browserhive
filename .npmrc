; openapi-typescript@7.x peers `typescript@^5.x`, but the project is on TS 6.
; The generator only emits plain `.ts` type declarations and works fine on TS 6,
; so we relax the peer-dependency check rather than pinning back to TS 5.
legacy-peer-deps=true
